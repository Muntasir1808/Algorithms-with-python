# tuples is similar to list but unlike list we can not modify tuples ..like we can not
# append,remove or insert or clear in tuples
numbers = (1, 1, 2, 3)
print(numbers.count(1))
# accessing is similar to list
print(numbers[0])
